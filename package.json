{
  "name": "clients",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@chakra-ui/react": "^1.8.8",              // Already compatible with React 17
    "@emoji-mart/data": "^1.2.1",              // Data package, no React peer dependency
    "@emoji-mart/react": "^1.1.1",             // Compatible with React 17
    "@emotion/react": "^11.10.6",              // Compatible with React 17
    "@emotion/styled": "^11.10.6",             // Compatible with React 17
    "@mui/material": "^5.10.17",               // Downgraded from 5.15.15 (React 18) to 5.10.17 (React 17)
    "@reduxjs/toolkit": "^1.9.7",              // Downgraded from 2.2.7 (React 18) to 1.9.7 (React 17)
    "@testing-library/jest-dom": "^5.17.0",    // Downgraded from 6.5.0 (React 18) to 5.17.0 (React 17)
    "@testing-library/react": "^12.1.5",       // Already compatible with React 17
    "@testing-library/user-event": "^14.5.2",  // Compatible with React 17
    "axios": "^1.7.7",                         // No React dependency, stays as is
    "dotenv": "^16.4.5",                       // No React dependency, stays as is
    "emoji-mart": "^5.6.0",                    // Compatible with React 17
    "framer-motion": "^6.5.1",                 // Already compatible with React 17
    "gapi-script": "^1.2.0",                   // Compatible with React 17
    "react": "^17.0.2",                        // Stays as specified
    "react-dom": "^17.0.2",                    // Stays as specified
    "react-google-login": "^5.2.2",            // Compatible with React 17
    "react-icons": "^4.12.0",                  // Downgraded from 5.3.0 (React 18) to 4.12.0 (React 17)
    "react-loading-skeleton": "^3.5.0",        // Compatible with React 17
    "react-notification-badge": "^1.5.1",      // Compatible with React 17
    "react-redux": "^7.2.9",                   // Already compatible with React 17
    "react-router": "^5.3.4",                  // Downgraded from 6.26.1 (React 18) to 5.3.4 (React 17)
    "react-router-dom": "^5.3.4",              // Downgraded from 6.26.1 (React 18) to 5.3.4 (React 17)
    "react-scripts": "^5.0.1",                 // Compatible with React 17
    "react-scrollable-feed": "^1.3.2",         // Compatible with React 17
    "react-toastify": "^9.1.3",                // Already compatible with React 17
    "react-transition-group": "^4.4.5",        // Compatible with React 17
    "socket.io-client": "^4.7.5",              // No React dependency, stays as is
    "three": "^0.167.1",                      // No React dependency, stays as is
    "web-vitals": "^3.5.2"                     // Downgraded from 4.2.3 (React 18) to 3.5.2 (React 17)
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "tailwindcss": "^3.4.13"                   // No React dependency, stays as is
  }
}
